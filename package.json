{
  "dependencies": {
    "create-react-app": "^5.1.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "scripts": {
    "start": "cd backend && node server.js",
    "build": "cd backend && npm install && cd ../oscar-frontend && npm install && npm run build"
  }
}